package com.knsr.wmgmt.controller;/*
package com.knsr.ingestion.controller;

import com.example.wmgmt.dto.request.MeterRequest;
import com.example.wmgmt.service.MeterService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.*;

@RestController
@RequestMapping("/v1/api/wmgmt/meters")
public class MeterController {
    @Autowired
    private MeterService meterService;

    @PostMapping
    public ResponseEntity<String> createMeter(@RequestBody MeterRequest request) {
        meterService.createMeter(request);
        return ResponseEntity.ok("Meter created successfully.");
    }
}
*/
